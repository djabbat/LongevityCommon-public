[package]
name = "aim-i18n"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "AIM i18n table — 7 locales (UN-6 + Georgian). Single source of truth for short user-facing strings shared between Rust binaries and the Phoenix UI."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
once_cell = "1"

[dev-dependencies]
