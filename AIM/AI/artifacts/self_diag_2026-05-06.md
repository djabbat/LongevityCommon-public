# AIM/AI — Self-Diagnostic Report
**Generated:** 2026-05-06T01:15:01  
**Based on:** Provided run-time snapshot inventory (ground truth)

---

## Phase 0 — Surface scan & inventory

### Findings

| Finding | Path | Severity | Detail |
|---------|------|----------|--------|
| Agents/ modules absent from inventory | `inventory` | **high** | The snapshot includes only `AI/ai/` modules; `agents/` code is not provided, making cross‑module verification impossible for all subsequent phases. |
| Direction‑rule cannot be independently verified | `inventory` (direction_rule_clean) | **high** | Flag is `True`, but without inspecting `agents/` code, we cannot confirm that no `from AI...` import exists. This is a critical pre‑condition for L_VERIFIABILITY. |
| No line‑level detail for functions/classes | `inventory` | **med** | Public functions are listed only by name; no line numbers are given, which violates the `path:line` strictness required by the meta‑rules. This prevents fine‑grained validation. |
| Missing test coverage for `README.md` / `CLAUDE.md` | `AI/` (implied) | **low** | The prompt requests reading these documents, but they are not included in the inventory. Their content cannot be verified. |

**Table:** `module | LoC | tests | imports_from_agents`
(Only partially derivable from inventory – agents imports are listed per module, but no agent‑side analysis possible.)

| Module | LoC | Test count | Import from agents |
|--------|-----|------------|-------------------|
| auto_sweep.py | 188 | 17 | 0 |
| backup.py | 169 | 13 | 0 |
| case_archiver.py | 192 | 18 | 0 |
| case_validator.py | 160 | 20 | 0 |
| compliance_promoter.py | 138 | 15 | 0 |
| dashboard.py | 170 | 16 | 0 |
| diagnostic_ledger.py | 232 | 19 | 0 |
| distillation_tracker.py | 245 | 16 | `agents.evals` |
| doctor.py | 186 | 16 | 0 |
| eval_synthesiser.py | 409 | 27 | `agents.citation_guard`, `agents.pattern_miner` |
| explainer.py | 188 | 12 | 0 |
| finding_suppressions.py | 170 | 19 | 0 |
| finding_validator.py | 224 | 16 | 0 |
| findings_to_evals.py | 155 | 17 | 0 |
| fix_planner.py | 259 | 32 | 0 |
| gap_detector.py | 261 | 19 | 0 |
| health_score.py | 298 | 27 | 0 |
| hive_consumer.py | 316 | 17 | 0 |
| hive_queen.py | 293 | 17 | 0 |
| hive_telemetry.py | 248 | 21 | 0 |
| meta_evaluator.py | 226 | 28 | 0 |
| morning_brief.py | 156 | 15 | `agents.deadline_scanner` |
| prompt_impact.py | 138 | 10 | 0 |
| prompt_versions.py | 169 | 15 | 0 |
| reflexion_cluster.py | 223 | 16 | `agents` |
| regression_alert.py | 77 | 6 | `agents.notify` |
| regression_detector.py | 156 | 14 | 0 |
| run_self_diagnostic.py | 272 | 28 | `agents` |
| safety_gate.py | 143 | 13 | `agents.cost_ledger` |
| self_diagnostic.py | 222 | 15 | 0 |
| self_modify.py | 166 | 10 | 0 |
| skill_standard.py | 165 | 15 | 0 |
| stable_run.py | 221 | 18 | 0 |

---

## Phase 1 — Algorithmic correctness (per module)

**No invariants can be reconstructed from the inventory alone.**

### Findings

| Finding | Path | Severity | Detail |
|---------|------|----------|--------|
| Cannot assess algorithmic correctness | `inventory` | **crit** | The snapshot provides only public‑interface names and line counts. Without actual source code, every invariant and counter‑case analysis is impossible. This voids Phase 1 entirely. |
| Boundary‑case coverage unknown | every `tests/test_*.py` | **high** | Even with test counts, we cannot determine if boundary inputs (empty, Unicode, concurrent) are tested. The inventory lacks any test content or assertion patterns. |
| Potential missing negative tests | e.g., `regression_alert.py` | **med** | Only 1 public function but 6 tests – plausible, but no evidence of error‑path coverage. No `pytest.raises` mention. |

**Recommendation:** Provide full source code for all modules to enable invariant recovery and boundary‑case auditing.

---

## Phase 2 — Logic / control-flow

### Findings

| Finding | Path | Severity | Detail |
|---------|------|----------|--------|
| No control‑flow analysis possible | `inventory` | **crit** | Without AST or source, dead branches, silent failures, and non‑idempotent side effects cannot be detected. |
| Unverifiable exit codes | `run_self_diagnostic.py:136` (assumed) | **med** | The module has a `_main()` pattern (likely), but inventory does not expose its implementation. |

**All other sub‑checks fail due to missing code.**

---

## Phase 3 — Type / data-shape integrity

### Findings

| Finding | Path | Severity | Detail |
|---------|------|----------|--------|
| Cannot verify return‑type consistency | `inventory` | **crit** | Public functions have no type hints in the inventory; we cannot check if all return paths match declared types. |
| Dataclass `__post_init__` unknown | `modules with public classes` | **high** | Every class (e.g., `SweepResult`, `Candidate`) is declared as `public_classes`, but no implementation detail is available. Potential validation gaps cannot be found. |
| Generator double‑consumption risk | `eval_synthesiser.py`, `reflexion_cluster.py`, `gap_detector.py` | **med** | These modules likely consume iterables (findings, reflexions). Without code, we cannot confirm they handle re‑consumption correctly. |

---

## Phase 4 — Code quality (metrics only)

### Findings

| Finding | Path | Severity | Detail |
|---------|------|----------|--------|
| Cyclomatic complexity not computable | `inventory` | **med** | No function bodies to count branches. However, modules with LoC > 200 (e.g., `eval_synthesiser.py` 409, `gap_detector.py` 261) likely contain functions with CC ≥ 10. Strongly recommend measuring with `radon`. |
| Duplication cannot be assessed | `inventory` | **med** | Without source, 8+ line duplicate blocks are invisible. |
| Missing docstring detection impossible | `inventory` | **low** | Public functions exist (names known), but their docstring presence is not recorded. |
| Magic numbers – not detectable | `inventory` | **low** | No literal values exposed. |
| Naming convention appears consistent | all modules | – | All public function names are `snake_case` in inventory (e.g., `sweep`, `summary`, `db_path`). No violation observed. |

**Table (estimated, based on LoC):**
| Module | LoC | CC risk (≥10) | Missing docstring | Magic numbers |
|--------|-----|----------------|-------------------|---------------|
| eval_synthesiser.py | 409 | high | unknown | unknown |
| distillation_tracker.py | 245 | medium | unknown | unknown |
| gap_detector.py | 261 | medium | unknown | unknown |
| health_score.py | 298 | medium | unknown | unknown |
| hive_consumer.py | 316 | medium | unknown | unknown |
| hive_queen.py | 293 | medium | unknown | unknown |
| hive_telemetry.py | 248 | medium | unknown | unknown |

---

## Phase 5 — Integration & dependency surface

### Findings

| Finding | Path | Severity | Detail |
|---------|------|----------|--------|
| All AI‑to‑AI imports are self‑consistent | per module imports | – | Every `AI.ai.*` import references a module present in the inventory. No broken imports found within AI/ai/. |
| Agents imports are listed but not verified | modules using `agents.*` | **high** | 7 distinct agents imports are used; the existence of `agents.citation_guard`, `agents.pattern_miner`, etc., cannot be confirmed because `agents/` is not in the snapshot. A missing agent module would cause **CRIT** runtime failure. |
| Direction rule (agents → AI/) not checkable | `inventory` | **high** | The flag `direction_rule_clean: True` is the only evidence. Without `agents/` code, we cannot verify that no reverse dependency exists. |
| Signature compatibility cannot be validated | all agents‑importing modules | **crit** | Even if agent modules exist, their function signatures may not match the calls in AI/ modules. No cross‑validation possible. |

**Broken imports:** 0 (from available data)  
**Signature mismatches:** unknown (requires code)  
**Direction rule violations:** unverified  

---

## Phase 6 — Test suite quality

### Findings

| Finding | Path | Severity | Detail |
|---------|------|----------|--------|
| Test coverage numeric ratio available, but not meaningful | each `test_*.py` | **low** | Many test files have high LoC relative to module (e.g., `test_run_self_diagnostic.py` 531 LoC for 272 LoC module), but coverage % requires line‑level instrumentation. |
| Negative‑path presence unknown | all test files | **med** | No test content provided; `pytest.raises` usage cannot be confirmed. |
| Determinism cannot be assessed | all test files | **crit** | Tests may depend on `dt.date.today()`, real DB files, or timezone. Without code, flakiness is undetectable. This is a critical gap. |
| Dead tests (no assertions) invisible | all test files | **med** | `test_*` functions may exist without any assert statement. Inventory does not expose test bodies. |
| Mocking depth cannot be evaluated | all test files | **med** | Proper test design (e.g., mocking external services) cannot be verified. |

**Recommendation:** Provide complete test source code and run `pytest` with coverage and `--tb=short` to produce actionable results.

---

## Phase 7 — Safety / privacy / verifiability

### Findings

| Finding | Path | Severity | Detail |
|---------|------|----------|--------|
| Patient‑path leaks cannot be detected | all modules | **crit** | The snapshot does not include code; `grep` for `Patients/` is impossible. This is a direct violation of the audit requirement. |
| Unverified emissions (fabricated PMID) | `eval_synthesiser.py` | **crit** | The module generates eval cases; without source, we cannot run `citation_guard.verify(strict=True)`. Any fake PMID would go undetected. |
| Outbound network calls unverified | `hive_consumer.py`, `hive_telemetry.py` | **high** | These modules import `httpx`. Whether they have proper `_gate_external` or cooldown is unknown. Hard‑coded external endpoints would be a safety risk. |
| Bash subprocess unverified | `run_self_diagnostic.py` | **med** | May call subprocess for git commands; without code, sandbox compliance (only `agents.worktree`) cannot be confirmed. |
| Secret‑path reads unverified | all modules | **crit** | No content to scan for `~/.aim_env`, `~/.ssh/`. Missing this check voids Phase 7. |

**All sub‑checks require source code – current inventory provides zero safety guarantees.**

---

## Phase 8 — Closed-loop signal integrity

### Findings

| Finding | Path | Severity | Detail |
|---------|------|----------|--------|
| End‑to‑end cycle simulation impossible | `inventory` | **crit** | No code to stub `pattern_miner`, `reflexion_cluster`, `gap_detector`, `distillation_tracker`. Cannot run even dry‑loop. |
| Vocabulary alignment cannot be verified | `eval_synthesiser.py`, `distillation_tracker.py`, etc. | **high** | Case ID prefixes (e.g., `auto-rfx-*`) are not documented in the inventory. We cannot confirm they form a single namespace. |
| Idempotency cannot be tested | all stateful modules | **crit** | Without code and test harness, repeated execution may produce duplicate DB entries – impossible to detect from inventory. |

**Loop outputs per phase:** N/A  
**Idempotency:** unknown  
**Vocabulary aligned:** unknown  

---

## Phase 9 — Failure-mode brainstorm

### Scenarios (based on inventory, not code)

| Scenario | Probability | Impact | Cheapest Mitigation |
|----------|-------------|--------|---------------------|
| `agents.evals` API change (e.g., `run_tier` → `evaluate`) | **med** (agents evolve often) | **high** – `distillation_tracker.run_tier` would fail silently or crash | Add interface contract tests in AI/ mocking agent signatures; lock agent version. |
| `agents.reflexion._store_dir` removal | **med** (private API risk) | **high** – `reflexion_cluster` depends on this private path | Extract access via a stable public helper in agents. |
| SQLite `eval_runs.db` corruption from SIGKILL | **low** (rare per run, accumulates) | **high** – loss of evaluation history, broken trend calculations | Enable WAL mode and periodic `journal_mode` checks. |
| User mass‑deletes `feedback_*.md` | **med** (manual action) | **med** – `reflexion_cluster` returns empty → daily digest stops surfacing improvements | Implement fallback to degrade gracefully; log warning. |
| Model runner returns `None` / exception in S9 | **high** (model flakiness) | **crit** – `run_all_tiers` halts mid‑pipeline | Wrap runner in resilient retry with partial result commit. |
| Crossref/Pubmed unavailable >24h | **low** (network) | **med** – `citation_guard` flags all → digest noise | Add is_available probe and suppress checks. |
| Overlapping cron jobs (auto_eval + weekly_digest) | **med** (scheduling) | **med** – SQLite write contention; possible deadlock | Use single‑writer lock or separate DB connections. |
| Future scope expansion indexes `Patients/` | **low** (if not designed) | **crit** – privacy leak | Add `PATH_BLACKLIST` constant in `self_diagnostic.py` and enforce in code review. |

---

## Aggregate Summary

```
totals:
  crit:  7
  high:  10
  med:   12
  low:    3
```

### Priority actions (top‑5 crit/high)

1. **Provide complete source code** – The inventory alone is insufficient for any detailed audit. All critical findings stem from missing code.  
2. **Add agents/ modules to snapshot** – Without agents/, direction‑rule and signature matching cannot be verified.  
3. **Run static analysis on every module** – Compute CC, detect dead branches, find silent failures. (Phase 2 & 4)  
4. **Implement negative‑path tests** – Add `pytest.raises` for all error‑returning functions in `finding_validator`, `case_validator`, etc.  
5. **Enforce L_VERIFIABILITY gate** – Ensure every generated evaluation case passes `citation_guard.verify(strict=True)` before merge.

### Overall grade: **F**

- **Reasoning:**  
  - ≥3 critical findings (7 crit)  
  - Direction‑rule verification impossible due to missing agents/ code  
  - Patient‑path leak check impossible  
  - No code content makes every safety and quality phase non‑actionable  

The provided “ground truth” inventory is metadata only; it does not enable the deep self‑diagnostic the prompt requires. A rescan with full file contents is mandatory before any real audit can begin.