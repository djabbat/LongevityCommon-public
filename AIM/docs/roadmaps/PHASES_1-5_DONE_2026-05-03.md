# AIM transformation 2026-05-02/03 — Phases 1-5 closed

Совокупный результат overnight-сессии. Цель: AIM > Claude Code, ведёт проекты, саморазвивается.

## Что закрыто

### Фаза 1 — Security parity (G1, G2, G3)

| ID | Что | Где |
|---|---|---|
| G1 (Python) | Bash sandbox: per-command flag policy + dangerous-token deny-list + shell=False везде | `agents/generalist.py` `_validate_bash`, `_BASH_FORBIDDEN_FLAGS`, `_BASH_DANGEROUS_TOKENS`; `_maybe_sandbox` без `/bin/sh -c` |
| G1 (Rust) | `BLOCKED_FLAG_PREFIXES` (prefix-match `--exec-path=` etc.); `git remote` removed | `rust-core/crates/aim-generalist/src/tools/bash_tool.rs` |
| G2 | Path sandbox: `_gate_path` (secret-path deny-list + `AIM_GENERALIST_ROOT` prefix-check) для read/view/write/edit/apply_patch | `agents/generalist.py` |
| G3 | Interactive permission broker: TUI/Telegram + cache + audit; opt-in через `AIM_INTERACTIVE_CONSENT=1` | `agents/permission.py` + `evaluate_l_consent` patch |

Тесты: **102** новых (51 bash + 31 path + 20 permission), регрессия 343/343 passed.

### Фаза 2 — Project ownership (P1, P2, P3, P4, P5)

| ID | Что | Где |
|---|---|---|
| P1 | Project Owner Agent: YAML schema, morning brief, hot-milestone & overdue detection | `agents/project_owner.py` + `USER/projects/FCLC.yaml` (pilot) |
| P2 | Calendar-aware deadline scanner: YAML + memory + Calendar adapter + horizon partitioner | `agents/deadline_scanner.py` |
| P3 | Stakeholder tracker: SQLite contacts DB + lifecycle hooks (on_email_sent/received/meeting) + sync_from_yaml | `agents/stakeholder_tracker.py` |
| P4 | Daily stand-up: render_brief + Telegram chunked send + systemd timer 09:00 | `scripts/daily_brief.py` + `deploy/systemd/aim-daily-brief.{service,timer}` |
| P5 | Project state machine: DRAFT→REVIEW→SUBMITTED→ACCEPTED→PUBLISHED + REJECTED/ARCHIVED + JSONL audit + per-phase next_actions | `agents/project_state_machine.py` |

Тесты: **81** новых.

### Фаза 3 — Eval foundation (S1)

| ID | Что | Где |
|---|---|---|
| S1 | Eval harness: 5 starter cases + 8 rubrics (regex/contains_all/contains_any/forbids/exact/json_keys/min_length/max_length) + SQLite eval_runs + compare(verA, verB) | `agents/evals.py` + `tests/evals/cases/*.yaml` |

Тесты: **19** новых.

### Фаза 4 — Self-improvement closed loop (S2, S3, S4, S5, S7)

| ID | Что | Где |
|---|---|---|
| S4 | Pattern miner: 5 minners (tool_failure_rate, slow_tool, redundant_memory_query, sequential_pair, error_type_frequency) | `agents/pattern_miner.py` |
| S5 | A/B routing tournament: Welch t-test + cost guard + decision audit + current_baseline | `agents/ab_router.py` |
| S3 | Prompt evolver: reflexion → mutate → eval-gated promote → versioned `~/.aim/prompts/<key>/v<n>.md` + revert | `agents/prompt_evolver.py` |
| S2 | Tool synthesis: template-only code gen из sequential_pair candidates + fixture-runner + register/remove | `agents/tool_synthesis.py` |
| S7 | Skill synthesis: N-gram mining + named YAML skills + invoke с param formatting + first-error stop | `agents/skill_synthesis.py` |
| S6 | (отложен — нельзя без накопления eval baseline) | — |

Тесты: **70** новых.

### Фаза 5 — Surpass features (G4, G6)

| ID | Что | Где |
|---|---|---|
| G4 | MCP-style extensibility: TOML config + JSON-RPC subprocess + e2e real-subprocess test | `agents/mcp_loader.py` + `~/.aim/mcp/*.toml` |
| G6 | delegate_parallel — оказался уже полноценным (не stub); regression test зафиксировал поведение | `tests/test_delegate_parallel.py` |
| G5 | Rust-stack streaming — отложен (Python streaming работает; не блокирующий) | — |

Тесты: **20** новых.

## Итог

**Новых модулей:** 11 (`agents/permission`, `project_owner`, `deadline_scanner`, `stakeholder_tracker`, `project_state_machine`, `evals`, `pattern_miner`, `ab_router`, `prompt_evolver`, `tool_synthesis`, `skill_synthesis`, `mcp_loader`).

**Test суммарно: 553 passed + 34 subtests** (full pytest suite, 37min runtime). До спринта: 311. Прирост: +242 теста + 34 subtests.

## Дополнительная волна 2026-05-03 (после Phases 1-5)

| ID | Что | Где |
|---|---|---|
| G5 | Rust-stack SSE streaming — оказался уже реализован (main.rs:95 `/v1/run/stream`); regression тест + serial guard | `rust-core/.../tests/streaming_tests.rs` (4) |
| W1 | git worktree isolation для self-modifying flows: `isolate()` ctx manager + бранч + cleanup_orphans | `agents/worktree.py` (15) |
| T1 | Telegram weekly self-improvement digest: pattern_miner + ab_router + prompt_evolver + tool/skill_synthesis + evals + memory_monitor | `scripts/weekly_digest.py` + systemd timer (6) |
| B1 | Smart morning preamble: greeting + hot milestone + overdue stakeholder + deadline horizon — auto-injected into daily brief | `agents/brief_preamble.py` (23) |
| M1 | Memory hygiene scanner: stale, obsolete deadlines, broken paths, duplicate descriptions | `agents/memory_monitor.py` (17) |
| P6 | Escalation rules engine: parse/eval YAML escalation_rules с tiny DSL (==, contains, and/or/not) + cooldown | `agents/escalation_engine.py` (23) |
| R1 | Citation guard: PMID/DOI/NCT/arXiv extraction + verify через PubMed/Crossref + strict mode + sanitize | `agents/citation_guard.py` (19) |
| D1 | Regimen validator: hard refusal на contraindicated/major pairs + annotate footer для doctor drafts | `agents/regimen_validator.py` (18) |
| N1 | Notification multiplexer: telegram/email/stdout/log + fallback chain + dedup window + audit | `agents/notify.py` (16) |
| A1 | Project archive: archive/unarchive с stamp suffix + autosweep idle-month candidates | `agents/project_archive.py` (13) |
| L1 | Cost ledger: daily/weekly/monthly windows + per-provider breakdown + budget alerts через notify | `agents/cost_ledger.py` (14) |
| U1 | Multi-language brief: RU/EN/KA с fallback на RU при unknown lang | `brief_preamble.py` extended |

**Дополнительная волна = +12 модулей, +172 теста (164 passed + 4 SSE + 4 не сосчитаны).**

## Третья волна 2026-05-03 (post-extension)

| ID | Что | Где |
|---|---|---|
| E1 | Email follow-up generator: per-stakeholder draft, 3 langs (EN/RU/KA), Gmail draft API | `agents/follow_up_generator.py` (13) |
| V1 | Vector recall CLI: thin wrapper над `memory_index.retrieve` + audit log | `agents/recall_cli.py` (11) |
| K1 | KPI tracker: floor/ceiling targets, velocity per week, bar render in brief | `agents/kpi_tracker.py` (17) |
| F1 | Reflexion auto-pull: orchestrator injects recent reflections в kwargs (env-gated) | `agents/orchestrator.py` patch (9) |
| G7 | Daily auto-eval с regression alert: cron 03:30, threshold 0.05, dedup 24h | `scripts/auto_eval.py` (6) |
| G8 | Single CLI dispatcher: `aim brief / recall / digest / followups / eval / project / memory / cost / escalate / health / version` | `aim_cli.py` (15) |
| G9 | Extended health snapshot: projects/eval/memory/cost/stakeholders/deadlines/cron + overall verdict | `agents/health_extended.py` (10) |

**Третья волна = +7 модулей, +81 тест.**

## Четвёртая волна 2026-05-03 (extension)

| ID | Что | Где |
|---|---|---|
| Q1 | Quick-action freeform CLI: `aim do "what's hot"` → 8 intents (brief/recall/followups/escalate/health/project_brief/transition/draft_email) | `agents/quick_action.py` (24) |
| B2 | Brief delivery preferences: lang/quiet_hours/channels/include/exclude в YAML | `agents/brief_preferences.py` + `USER/preferences/brief.yaml` (20) |
| D2 | Doctor calibration tracker: predictions DB + Brier score + per-bucket calibration + bias detection | `agents/doctor_calibration.py` (15) |
| L2 | Literature watch: PubMed esearch+esummary + dedup against own publications, 6h cooldown | `agents/literature_watch.py` + `USER/preferences/literature.yaml` (14) |
| N2 | Rate limiter в notify multiplexer: max N/hour, high-level bypass | `notify.py` extended (4 tests) |
| P7 | KPI auto-update: hook signals (cost/stakeholders/eval/memory/literature) → push в YAML | `agents/kpi_auto_updater.py` (13) |

**Четвёртая волна = +6 модулей, +90 тестов.**

## Пятая волна 2026-05-03 (operational layer)

| ID | Что | Где |
|---|---|---|
| G10 | `aim serve` long-running daemon: tick scheduler (daily/weekly/every) + Unix socket server для Telegram bot integration | `agents/serve_daemon.py` (17) |
| DA1 | Git diff analyser: porcelain → bucket classifier (test/feat/fix/docs/build/style/...) → conventional-commit suggester с optional LLM polish | `agents/diff_analyser.py` (24) |
| PR1 | Auto README generator: YAML + memory + git log + KPI + phase actions → markdown skeleton с polish hook | `agents/readme_generator.py` (14) |

**Пятая волна = +3 модуля, +55 тестов.**

## Шестая волна 2026-05-03 (safety + perf)

| ID | Что | Где |
|---|---|---|
| DR1 | Doctor dry-run pre-emit pass: citation_guard sanitize + regimen_validator hard refusal в одном call + audit | `agents/doctor_dry_run.py` (12) |
| RT1 | Project dependency graph: explicit `depends_on` + soft refs (blocker/goal/note) → DOT/Mermaid/adjacency + cycle detection | `agents/project_graph.py` (15) |
| SL1 | Slow recall query detector + LRU cache: install() patch на memory_index.retrieve, slow threshold 500ms, JSONL audit, hot-query cache | `agents/recall_perf.py` (13) |

**Шестая волна = +3 модуля, +40 тестов.**

## Седьмая волна 2026-05-03 (composability)

| ID | Что | Где |
|---|---|---|
| EX1 | Project export bundle: zip с YAML + README + brief + memory + git_log + manifest для emails co-PI / backup | `agents/project_export.py` (11) |
| FX1 | Feature-flag/experiment tracker: SQLite registry + cleanup_by + status (active/ramping/ready_to_remove/retired) + overdue surfacing | `agents/feature_flags.py` (17) |
| RB1 | Named routines: YAML-defined bundles `{morning, pre-grant-submit, weekly-review, health-quick}` + `aim routine run <name>` + audit | `agents/routines.py` + `USER/preferences/routines.yaml` (12) |

**Седьмая волна = +3 модуля, +40 тестов.**

## Восьмая волна 2026-05-03 (medical/codebase glue)

| ID | Что | Где |
|---|---|---|
| PA1 | Patient INBOX → patient folder mover: OCR + name/DOB extraction + AI_LOG.md trail; sentinel `2000_01_01` для unknown DOB | `agents/patient_inbox_watcher.py` (18) |
| SC1 | Source code change-impact analyser: AST imports → reverse map → "after touching X, run these tests" recommendation | `agents/impact_analyser.py` (15) |

**Восьмая волна = +2 модуля, +33 теста.**

## Девятая волна 2026-05-03 (insight layer)

| ID | Что | Где |
|---|---|---|
| PV1 | Crossref author watcher: pull новых публикаций по AIM_AUTHOR_NAME, dedup against publications.md, surface в digest | `agents/own_pubs_tracker.py` (9) |
| CT1 | Calendar conflict detector: same-day deadline crowding в configurable window | `agents/deadline_scanner.py` extension (5) |
| SE1 | Session timeline visualiser: JSONL → markdown timeline + per-tool stats + interesting events | `agents/session_visualiser.py` (13) |

**Девятая волна = +3 модуля, +27 тестов.**

## Десятая волна 2026-05-03 (consult + presentation + safety)

| ID | Что | Где |
|---|---|---|
| DC1 | Doctor consult delegate: structured Intake → differential → regimen → dry_run → calibration log в одном вызове | `agents/doctor_consult.py` (12) |
| PE1 | Project export → HTML/PDF: pandoc when available, pure-Python markdown→HTML fallback (lists, code, emphasis, escape) | `agents/project_pdf_export.py` (12) |
| UN1 | Unicode/lang validator: NFC normalisation + zero-width strip + mixed-script detector (homograph attack) + safe() | `agents/unicode_guard.py` (23) |

**Десятая волна = +3 модуля, +47 тестов.**

## Одиннадцатая волна 2026-05-03 (UX + repo hygiene)

| ID | Что | Где |
|---|---|---|
| G11 | bash + zsh tab-completion: project / routine / phase names dynamic | `agents/cli_completion.py` (11) |
| DD1 | Patient encounter duplicate detector: NFC + lowercase + homograph fold + exact-DOB + sentinel-DOB clusters | `agents/patient_dedup.py` (13) |
| PR2 | Citation linter for project markdown: walk repo, run citation_guard.verify, report unresolved by file:line | `agents/citation_linter.py` (13) |

**Одиннадцатая волна = +3 модуля, +37 тестов.**

## Двенадцатая волна 2026-05-03 (introspection)

| ID | Что | Где |
|---|---|---|
| G12 | Module registry: AST walk → capability map (description, public API, imports). Real codebase: **128 modules** | `agents/module_registry.py` (17) |
| RM1 | Memory remediator: rglob hunt for broken-path basenames + component-scoring. Real run: 29 high-confidence fixes из 127 broken paths | `agents/memory_remediator.py` (14) |

**Двенадцатая волна = +2 модуля, +31 тест.**

## Final tally (всё с 2026-05-02)

- **57 новых модулей** в `agents/` + `scripts/` + top-level `aim_cli.py`.
- **3 systemd timer'а** + **`aim serve` daemon** с Unix socket interface.
- **5 starter eval cases**, **5 literature queries**, **4 routine bundles**, **FCLC pilot config** с escalation_rules + stakeholders + KPI hooks.
- **~900 новых тестов** (242 phases 1-5 + 164 wave 2 + 81 wave 3 + 90 wave 4 + 55 wave 5 + 40 wave 6 + 40 wave 7 + 33 wave 8 + 27 wave 9 + 47 wave 10 + 37 wave 11 + 31 wave 12 + sundry).
- Real run: AIM видит 13 deadlines today, 127 broken memory paths, эскалирует Tsomaia phase 1 ordering, генерит conventional-commit suggestion для своего собственного состояния (264 added, 52 modified files), и автогенерирует README для FCLC с актуальными milestones/stakeholders/blockers.

**Артефакты деплоя:** systemd timer для daily brief.

**Pilot config:** `USER/projects/FCLC.yaml` — реальная FCLC-конфигурация с Geiger/Miguel/Janke/Preneel/Tsomaia/Zhvania/Shashviashvili stakeholders + EIC Oct 28 deadline + audit blockers.

## Где AIM теперь сильнее Claude Code

| Capability | Claude Code | AIM (после этого спринта) |
|---|---|---|
| Project ownership | reactive | proactive: own YAML state, morning brief, deadline horizon, stakeholder follow-ups |
| Eval-driven self-improvement | none | full pipeline: reflexion → patch → eval → promote (statistically gated) |
| Tool extensibility | MCP servers | MCP servers + tool synthesis + skill synthesis |
| Pattern mining of own logs | none | 5 distinct miners |
| Multi-strategy routing | single model | A/B tournament with Welch t-test + cost guard |
| Permission UX | per-tool prompts | per-action broker with cache, audit, multi-channel (TUI/TG) |
| Domain-specific agents | skills | doctor + intake + lang + writer + researcher + coder + email + project_owner |

## Где Claude Code всё ещё впереди

- Permission UX зрелость (CC built-in to harness; AIM прокси через broker).
- Worktree isolation для side-effect experimentation (AIM не имеет git-worktree integration).
- Streaming в Rust-стэке (G5 не закрыт).

## Следующие фазы (на потом)

- **S6 Code self-modification** — после ≥4 weeks of accumulated eval runs (need baseline distribution).
- **G5 Rust streaming** — `axum::sse` + tokio channels.
- **Worktree-based safe experimentation** для S2/S6 (run synthesised code in isolated git worktree).
- Real-world telemetry: подключить `pattern_miner.summary()` в weekly Telegram digest.

## Команды быстрой проверки

```bash
cd ~/Desktop/LongevityCommon/AIM
~/Desktop/LongevityCommon/AIM/venv/bin/python -m agents.project_owner FCLC
~/Desktop/LongevityCommon/AIM/venv/bin/python -m agents.deadline_scanner
AIM_TG_DRYRUN=1 ~/Desktop/LongevityCommon/AIM/venv/bin/python -m scripts.daily_brief
~/Desktop/LongevityCommon/AIM/venv/bin/python -m pytest tests/ -q
```
