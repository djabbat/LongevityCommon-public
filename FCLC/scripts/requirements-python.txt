# FCLC — Python Dependencies
# Установка: python3 -m venv venv && source venv/bin/activate && pip install -r requirements-python.txt
# Назначение: OMOP ETL, DeepSeek API, валидация датасетов, анализ результатов федерации

# ── LLM / AI (DeepSeek API) ───────────────────────────────
openai>=1.14.0             # DeepSeek API (OpenAI-compatible endpoint)

# ── Конфигурация ───────────────────────────────────────────
python-dotenv>=1.0.0       # Загрузка ~/.aim_env (DEEPSEEK_API_KEY)

# ── OMOP CDM / База данных ────────────────────────────────
pandas>=2.0.0              # Обработка таблиц OMOP (person, condition_occurrence, measurement)
sqlalchemy>=2.0.0          # ORM для PostgreSQL
psycopg2-binary>=2.9.0    # PostgreSQL драйвер (fclc-server БД)
pydantic>=2.0.0            # Валидация OMOP-схем, конфигурация узлов

# ── HTTP / REST ────────────────────────────────────────────
requests>=2.31.0           # HTTP-клиент для REST API fclc-server

# ── Математика / ML ───────────────────────────────────────
numpy>=1.26.0              # Обработка градиентов, Shapley-аппроксимации
scikit-learn>=1.4.0        # AUC/ROC для MVP-валидации (T2DM AUC > 0.75)

# ── Визуализация ───────────────────────────────────────────
matplotlib>=3.8.0          # Графики результатов раундов, кривые обучения

# ── Медицинские данные ────────────────────────────────────
pdfplumber>=0.10.0         # Извлечение данных из медицинских PDF (эпикризы, анализы)

# ── Тестирование ──────────────────────────────────────────
pytest>=8.0.0              # Тесты Python-скриптов
pytest-asyncio>=0.23.0    # Асинхронные тесты
